# Vue Frontend Lab

This Vue application is embedded in `Portfolio.Web` as the `<vue-showcase>`
custom element. Its production build is written to
`Portfolio.Web/wwwroot/showcases/vue`.

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run build
```

